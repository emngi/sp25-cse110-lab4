1. Line 9 will print `values added: 20`
2. Line 13 will print `final result: 20`
3. You should not use var because it is usually global or function scoped and can cause naming conflicts.
4. Line 9 prints 'values added: 20`
5. Line 13 will throw a reference error since result is declared in the if conditional, so it cannot be accessed outside this code block.
6. Line 9 will throw a type error since the cost type variable is trying to bereassigned at line 7.
7. Line 13 will not run since we throw an error at line 7. Even if it did, it would throw an error again since cosnt only exists in that block.